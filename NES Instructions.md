NES 硬件工作原理详解 —— 模拟器开发参考

本文档从模拟器开发者的角度，系统梳理 NES（Nintendo Entertainment System / Famicom）的硬件架构与工作原理。内容主要参考 NESdev Wiki 技术报告、FCEUX 核心仿真系统以及 fogleman/nes 的组件化实现
1. 系统总览

NES 是一个紧耦合的 8-bit 系统，由三大核心芯片构成，均运行在同一主时钟域下：
┌─────────────────────────────────────────────────────────────┐
│                       NES System                            │
│                                                             │
│  ┌──────────────┐        ┌──────────────┐                   │
│  │   CPU        │◄──────►│   PPU        │──────► Video Out  │
│  │  (2A03/6502) │  Bus   │  (2C02)      │     256x240       │
│  │  1.79 MHz    │        │  5.37 MHz    │                   │
│  └──────┬───────┘        └──────┬───────┘                   │
│         │                       │                           │
│         │  ┌──────────────┐     │                           │
│         └─►│   APU        │     │                           │
│            │  (2A03 内置) │     │                           │
│            └──────┬───────┘     │                           │
│                   │             │                           │
│            ┌──────▼─────────────▼───────┐                   │
│            │        Cartridge           │                   │
│            │  PRG ROM | CHR ROM | Mapper│                   │
│            └────────────────────────────┘                   │
│                                                             │
│  ┌──────────────┐        ┌──────────────┐                   │
│  │  Controller  │───────►│   CPU RAM    │                   │
│  │  (Input)     │  I/O   │   2 KB       │                   │
│  └──────────────┘        └──────────────┘                   │
└─────────────────────────────────────────────────────────────┘

时钟关系（NTSC 制式）：
组件	时钟频率	与 CPU 时钟比
CPU	1.789773 MHz	1×
PPU	5.369318 MHz	3×
APU	1.789773 MHz	1×

核心调度原则：每个 CPU 周期，PPU 执行 3 个周期（dots），APU 执行 1 个周期。在周期精确模拟器中，调度器以 PPU dot 为最小时间单位，同步推进所有组件。

2. CPU：MOS 6502 核心
2.1 芯片概述

NES 使用的 CPU 是 Ricoh 2A03（PAL 版为 2A07），其核心是 MOS 6502 的变体。它与标准 6502 的关键区别在于：移除了 BCD（二进制编码十进制）指令，并内置了 APU（伪音频处理单元）。

6502 是 8-bit 微处理器，非流水线设计，内部寄存器极少，不支持乘法等复杂运算。
2.2 寄存器结构

┌──────────────────────────────────────────────┐
│             6502 CPU Registers               │
├──────────────────────────────────────────────┤
│  PC  (Program Counter)    16-bit  0x0000-FFFF│
│  SP  (Stack Pointer)       8-bit  0x0100-01FF│
│  A   (Accumulator)         8-bit              │
│  X   (Index Register X)    8-bit              │
│  Y   (Index Register Y)    8-bit              │
├──────────────────────────────────────────────┤
│              Status Register (P)             │
│  N V - B D I Z C                             │
│  │ │   │ │ │ │ │                             │
│  │ │   │ │ │ │ └─ Carry                       │
│  │ │   │ │ │ └─── Zero                        │
│  │ │   │ │ └───── Interrupt Disable           │
│  │ │   │ └─────── Decimal (NES 中无效)         │
│  │ │   └───────── Break                       │
│  │ └───────────── Overflow                    │
│  └─────────────── Negative                    │
└──────────────────────────────────────────────┘

在模拟器中，CPU 结构体通常包含以上所有寄存器以及一个指令函数指针表。
2.3 指令执行流程

CPU 的执行遵循标准的取指-译码-执行循环：
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  检查中断   │───►│  读取操作码 │───►│  确定寻址   │───►│  执行指令   │
│  (NMI/IRQ)  │    │  (PC++)     │    │  方式并计算 │    │  更新状态   │
│             │    │             │    │  有效地址   │    │  更新周期   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘

6502 支持 56 条官方指令，多种寻址模式（立即数、零页、绝对、间接、索引等）。FCEUX 和 fogleman/nes 都实现了完整的官方指令集，部分模拟器还实现了未公开的非法指令。
2.4 中断系统

NES 的 6502 支持三种中断源：
中断类型	触发源	向量地址	可屏蔽
NMI (非屏蔽中断)	PPU 在每帧 VBlank 开始时触发	$FFFA	否
IRQ (中断请求)	Mapper 硬件 / APU 等	$FFFE	是（通过 I 标志位）
RESET	开机 / 复位按钮	$FFFC	否

中断的轮询发生在大多数指令的倒数第二个周期结束时，这是一个对模拟器精度影响很大的细节。

3. PPU：Ricoh 2C02 图形处理单元

PPU 是整个模拟器中最复杂的组件。它负责将 CHR ROM 中的图案数据、Nametable 中的布局信息以及调色板数据合成为 256×240 像素的视频输出。
3.1 PPU 内部结构

┌─────────────────────────────────────────────────────────┐
│                    PPU (2C02)                            │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │  Pattern    │  │  Name       │  │  Palette    │     │
│  │  Tables     │  │  Tables     │  │  (32 bytes) │     │
│  │  (8KB)      │  │  (2KB VRAM) │  │             │     │
│  │  ┌───┬───┐  │  │  ┌───┬───┐  │  └─────────────┘     │
│  │  │BG │SP │  │  │  │NT0│NT1│  │                       │
│  │  │   │   │  │  │  ├───┼───┤  │  ┌─────────────┐     │
│  │  └───┴───┘  │  │  │NT2│NT3│  │  │  OAM        │     │
│  │   (CHR)     │  │  └───┴───┘  │  │  (64 Sprites│     │
│  │             │  │  (Mirroring)│  │   × 4 bytes)│     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │           渲染流水线 (8 个 8-pixel 移位器)        │   │
│  │  BG Shift Reg A | BG Shift Reg B | SP Shift Reg  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │           8 个内存映射寄存器 ($2000-$2007)        │   │
│  │  PPUCTRL | PPUMASK | PPUSTATUS | OAMADDR        │   │
│  │  OAMDATA | PPUSCROLL | PPUADDR | PPUDATA        │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘

PPU 通过 8 个内存映射寄存器（$2000-$2007）与 CPU 通信。
3.2 视频内存架构

PPU 拥有独立的地址空间，通过 PPU 总线访问：
PPU 地址空间 ($0000-$3FFF)
┌──────────────┬──────────────┬──────────────────────────┐
│  $0000-$0FFF │  $1000-$1FFF │  $2000-$2EFF            │
│  Pattern     │  Pattern     │  Name Table + Attribute │
│  Table 0     │  Table 1     │  (2KB VRAM, 可镜像)      │
│  (4KB CHR)   │  (4KB CHR)   │                          │
├──────────────┼──────────────┼──────────────────────────┤
│  $3000-$3EFF │  $3F00-$3F1F │  $3F20-$3FFF            │
│  Name Table  │  Palette     │  Palette Mirror          │
│  镜像        │  (32 bytes)  │                          │
└──────────────┴──────────────┴──────────────────────────┘

Pattern Table 存放 8×8 像素的 tile 数据，每个 tile 占 16 字节（64 个 2-bit 像素索引）。每个 Pattern Table 含 256 个 tile，共 4KB。像素的 2-bit 值是调色板中的颜色索引（0-3）。

Name Table 存放背景的 tile 索引布局，32×30 个 tile 对应屏幕的 256×240 像素。Name Table 支持四种镜像模式（水平、垂直、单屏低、单屏高），由 Mapper 控制。

3.3 渲染流水线

PPU 以 scanline 和 dot 为时间单位进行渲染。NTSC 制式下每帧有 261 条 scanline，每条 scanline 341 个 dot：
一帧的时间线 (NTSC)
Scanline 0 ──────────────────────────── 240   ── 241 ── 260 ── 261
          │◄──── 可见区域 (256×240) ────►│     │      │       │
          │                              │ VBlank │  VBlank│ Pre-render
          │                              │ 开始   │        │
Dot:      0    1    ...   256  257 ... 340
          │    │            │    │
          │    │            │    └── 水平消隐 (HBlank)
          │    └── Tile 0 渲染开始
          └── 背景 Tile 获取

每个可见 Scanline 的 dot 分配：
Dots 0-255:   渲染像素
Dots 256-340: 获取下一行的 sprite，水平消隐
每个 CPU 周期，PPU 执行 3 个 dot，因此调度器需要以 PPU dot 为最小精度推进。

3.4 背景渲染

背景渲染涉及两个关键移位寄存器（A 和 B），分别存放 tile 的低位和高位 bit-plane：
每个 dot 周期：
1. 从 Pattern Table 获取 tile 的低位/高位字节
2. 加载到 BG 移位寄存器 (A 和 B)
3. 每 dot 移位一次，从 MSB 取出 2-bit 像素值
4. 结合 Attribute 数据选择调色板
5. 从调色板取出最终颜色索引

3.5 精灵渲染

NES 支持 64 个精灵，每个精灵由 4 字节 OAM 条目描述：Y 坐标、tile 索引、属性（调色板、优先级、翻转）、X 坐标。

精灵渲染的关键机制：

    每 scanline 最多 8 个精灵，超出则设置精灵溢出标志

    精灵 0 命中检测：当精灵 0 与背景像素在同一位置非透明时触发，常用于分割屏幕

    支持 8×8 和 8×16 两种精灵尺寸模式

3.6 VBlank 与 NMI

PPU 在 scanline 241 的 dot 1 设置 VBlank 标志位，并触发 NMI 中断到 CPU。游戏通常在 VBlank 期间进行 VRAM 和 OAM 的更新，因为此时 PPU 不访问 VRAM。

4. 内存映射与总线
4.1 CPU 地址空间

CPU 拥有 16-bit 地址总线（64KB 地址空间），但数据总线仅 8-bit：

CPU 地址空间 ($0000-$FFFF)
┌──────────────┬──────────────┬──────────────────────────────┐
│  $0000-$07FF │  $0800-$1FFF │  $2000-$401F                │
│  CPU RAM     │  RAM 镜像    │  I/O 寄存器                  │
│  (2KB)       │  (每 2KB    │  ($2000-$2007: PPU)         │
│              │   重复)      │  ($4014: OAMDMA)            │
│              │              │  ($4016: 手柄)               │
├──────────────┼──────────────┼──────────────────────────────┤
│  $4020-$FFFF │              │                              │
│  Cartridge   │              │  PRG ROM (Mapper 控制)       │
│  Space       │              │                              │
└──────────────┴──────────────┴──────────────────────────────┘

关键设计原则：NES 中几乎所有组件都是内存映射的。CPU 通过读写特定地址的寄存器来与 PPU、APU、手柄等通信。

4.2 总线架构

┌─────────┐     ┌─────────┐     ┌─────────┐
│  CPU    │     │  PPU    │     │  APU    │
│  Bus    │     │  Bus    │     │  Bus    │
└────┬────┘     └────┬────┘     └────┬────┘
     │               │               │
     ▼               ▼               ▼
┌─────────────────────────────────────────┐
│              System Bus                 │
│  ┌─────────┐ ┌─────────┐ ┌───────────┐ │
│  │ CPU RAM │ │ VRAM    │ │ Cartridge │ │
│  │ (2KB)   │ │ (2KB)   │ │ (PRG+CHR) │ │
│  └─────────┘ └─────────┘ └───────────┘ │
└─────────────────────────────────────────┘

CPU 总线和 PPU 总线是独立的。CPU 通过 PPU 的 8 个寄存器间接访问 PPU 的地址空间，每次访问需要设置 PPUADDR 寄存器。

5. APU：音频处理单元

APU 位于 2A03 芯片内部，包含 5 个音频通道：
┌─────────────────────────────────────────────┐
│                  APU                         │
│  ┌─────────────┐  ┌─────────────┐           │
│  │ Pulse 1     │  │ Pulse 2     │           │
│  │ (方波,占空比)│  │ (方波,占空比)│           │
│  └──────┬──────┘  └──────┬──────┘           │
│         │                │                   │
│  ┌──────▼──────┐  ┌──────▼──────┐           │
│  │ Triangle    │  │ Noise       │           │
│  │ (三角波)    │  │ (噪声)      │           │
│  └──────┬──────┘  └──────┬──────┘           │
│         │                │                   │
│  ┌──────▼──────┐                            │
│  │ DMC         │                            │
│  │ (DPCM 采样) │                            │
│  └──────┬──────┘                            │
│         │                                    │
│  ┌──────▼──────────────────────────────┐    │
│  │         Mixer (非线性混合)           │    │
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘

通道	波形	特点
Pulse 1	方波	可调占空比（12.5%/25%/50%/75%），支持频率扫描
Pulse 2	方波	同上
Triangle	三角波	无音量控制，32 步阶梯波
Noise	噪声	伪随机序列，支持多种周期模式
DMC	DPCM	播放采样音频，可触发 DMA 从 CPU 内存读取采样数据

APU 的 DMC 通道触发 DMA 时会暂停 CPU，这是模拟器中需要精确处理的时序交互。


6. Mapper：卡带地址映射

Mapper 是卡带中的地址映射芯片（Memory Management Circuit），负责将 CPU/PPU 的地址映射到卡带 ROM 的物理地址。
6.1 为什么需要 Mapper

NES 的 CPU 地址空间只有 64KB，PPU 的 CHR 地址空间只有 8KB。但游戏 ROM 可以远超这些限制。Mapper 通过分页（bank switching） 机制，让游戏可以在运行时动态切换映射到地址空间的 ROM 段。
6.2 常见 Mapper


Mapper	名称	使用游戏	特性
000	NROM	超级马里奥兄弟	最简单的映射，无 bank switching
001	MMC1	塞尔达传说	串行写入，支持 PRG/CHR 分页
002	UxROM	魂斗罗	PRG 分页，CHR 固定
003	CNROM	一些早期游戏	仅 CHR 分页
004	MMC3	超级马里奥兄弟 3	PRG/CHR 分页，扫描线 IRQ 计数器

MMC3 的扫描线 IRQ 是一个关键特性：它通过监听 PPU 地址线的 A12 信号来计数扫描线，在第 N 条扫描线触发 IRQ 中断，用于实现分屏效果。

7. 模拟器架构参考
7.1 组件化设计

参考 fogleman/nes 的架构，模拟器核心由以下组件构成：
┌─────────────────────────────────────────────────┐
│                   Console                       │
│  (中央协调器，持有所有组件引用)                    │
│                                                 │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌────────────┐ │
│  │ CPU  │  │ PPU  │  │ APU  │  │ Cartridge  │ │
│  └──┬───┘  └──┬───┘  └──┬───┘  └─────┬──────┘ │
│     │         │         │             │        │
│     │    ┌────▼────┐    │        ┌────▼────┐   │
│     └───►│  Bus    │◄───┘        │ Mapper  │   │
│          │ (内存映射)│             │ (地址映射)│   │
│          └─────────┘             └─────────┘   │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐            │
│  │ Controller 1 │  │ Controller 2 │            │
│  └──────────────┘  └──────────────┘            │
└─────────────────────────────────────────────────┘

7.2 主仿真循环
Console.Step() {
    1. CPU.Step()           // 执行一条指令（可能消耗多个周期）
    2. for (3 × CPU周期数):
           PPU.Step()       // 每个 CPU 周期推进 3 个 PPU dot
    3. for (每个 CPU 周期):
           APU.Step()       // 每个 CPU 周期推进 1 个 APU 周期
    4. Mapper.Step()        // 与 PPU 同步
}

fogleman/nes 的 Console.Step()、StepFrame() 方法实现了这一主循环。

7.3 时序模型选择

模拟器开发中有时序精度递进的选择：
级别	精度	说明
1	每条指令	最快，但兼容性最差
2	每个 CPU 周期	中等精度，适用于大部分游戏
3	CPU 周期 + PPU dot	高精度，推荐用于正式项目
4	主时钟 / 信号级	最高精度，用于极限精确模拟

对于运行《超级马里奥》等目标，级别 3（CPU 周期 + PPU dot） 是推荐的起点。

8. 关键测试资源
资源	用途
nestest.nes	CPU 指令集和时序的黄金标准测试 ROM
NESdev Wiki	最权威的 NES 硬件文档库
FCEUX 调试器	内置 CPU/PPU 调试器，可用于对比行为
fogleman/nes	结构清晰的 Go 语言参考实现

9. 实现建议

从 CPU 开始：先用 nestest 验证 6502 核心的指令集和时序正确性。
实现 PPU 的渲染流水线：优先支持背景渲染，再逐步加入精灵和滚动。
使用周期精确调度：以 PPU dot 为最小时间单位，同步所有组件。
Mapper 从 NROM（000）开始：先让最简单的游戏跑起来，再扩展 MMC1/MMC3。
APU 可以后置：许多模拟器在早期版本中暂时跳过 APU，优先确保图像正确。
始终以测试 ROM 为准：nestest、ppu_vbl_nmi、sprite_hit_tests 等测试 ROM 是验证正确性的最佳手段。